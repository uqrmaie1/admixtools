% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{compute_f2_cache_id}
\alias{compute_f2_cache_id}
\title{Compute a stable cache identifier for an \code{extract_f2} run}
\usage{
compute_f2_cache_id(
  pref,
  format = NULL,
  inds = NULL,
  pops = NULL,
  snpfile_kept,
  blgsize = 0.05,
  extra_args = list()
)
}
\arguments{
\item{pref}{Genotype prefix (same as passed to \code{\link[=extract_f2]{extract_f2()}}).}

\item{format}{One of \code{'plink'}, \code{'packedancestrymap'}, \code{'eigenstrat'}, or
\code{NULL} to detect from file existence at \code{pref}.}

\item{inds, pops}{Individual / population filters (same as passed to \code{\link[=extract_f2]{extract_f2()}}).}

\item{snpfile_kept}{A data.frame with at least \code{CHR}, \code{POS}, \code{A1}, \code{A2}
columns, representing the SNPs that survived filtering.}

\item{blgsize}{Block size in Morgans (same as passed to \code{\link[=extract_f2]{extract_f2()}}).}

\item{extra_args}{Named list of additional \code{extract_f2} arguments whose
values affect output. Captured verbatim.}
}
\value{
A character scalar of the form \code{"sha256:<64 hex chars>"}.
}
\description{
Returns a string of the form \code{"sha256:<hex>"} that uniquely identifies the
inputs that determined the f2 blocks produced by \code{\link[=extract_f2]{extract_f2()}}. Two runs
over byte-equivalent inputs (same indfile, same variants, same arguments,
same \code{admixtools} version) produce byte-equal hashes; any change to any
contributing input changes the hash.
}
\details{
When \code{\link[=extract_f2]{extract_f2()}} is given an \code{outdir}, the cache id is also written as
a single line to \verb{<outdir>/.f2_cache_id}. Orchestrators can read this file
to decide whether previously cached downstream artefacts (qpAdm / qpGraph
results computed against these blocks) are still current, without having
to hash hundreds of MB of \code{.rds} block files or rely on mtimes.

The hash is computed from:
\itemize{
\item \strong{Sample set} — sorted list of IIDs that contributed to the f2 blocks,
resolved from the input \code{.fam} / \code{.ind} / \code{.psam} according to \code{inds} and
\code{pops}.
\item \strong{Variant set} — sorted \verb{(CHR, POS, A1, A2)} tuples of the SNPs that
survived all filtering (\code{auto_only}, \code{transitions}, \code{transversions},
\code{keepsnps}, \code{maxmiss}, \code{minmaf}, \code{maxmaf}, \code{minac2}).
\item \strong{Block boundaries} — per-block lengths produced by \code{\link[=get_block_lengths]{get_block_lengths()}}
with the supplied \code{blgsize}.
\item \strong{Package version} — \code{utils::packageVersion('admixtools')}.
\item \strong{Filter arguments} — the additional \code{extract_f2} arguments that affect
which SNPs are kept and how blocks are formed.
}
}
\seealso{
\code{\link[=extract_f2]{extract_f2()}}
}
