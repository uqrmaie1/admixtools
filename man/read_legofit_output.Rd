% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legofit.R
\name{read_legofit_output}
\alias{read_legofit_output}
\title{Read a LEGOFIT fitted-output file into an admixtools edge tibble}
\usage{
read_legofit_output(path, graph = NULL, include_fixed = TRUE)
}
\arguments{
\item{path}{Path to a \code{.legofit} file (legofit stdout, UTF-8).}

\item{graph}{An edge tibble, igraph, or \code{NULL}. When supplied, parameter
names are mapped to admixtools edge endpoints per the segment-naming
convention from \code{\link[=graph_to_lgo]{graph_to_lgo()}}. When \code{NULL}, returns a raw parameter
table.}

\item{include_fixed}{Logical (default \code{TRUE}). When \code{TRUE}, fixed parameter
values from the \strong{Initial parameter values} block are included in the
result. When \code{FALSE}, only free-fitted values are returned.}
}
\value{
A tibble (with \code{node_times} and \code{fit_convergence} attributes) when
\code{graph} is supplied; a raw parameter tibble (with \code{fit_convergence}) when
\code{graph = NULL}.
}
\description{
Parses the stdout of a \code{legofit} run into an admixtools edge tibble with
fitted parameter values attached as additional columns. When \code{graph} is
supplied, parameters are mapped to edges by segment name; when \code{NULL}, a
raw parameter tibble is returned.
}
\section{Parameter naming convention}{

Family classification (the \code{family} column, and the edge mapping when \code{graph}
is supplied) keys off the \verb{[graph_to_lgo()]} naming convention: \verb{T_<node>}
(split time), \verb{T_admix_<dest>} (admix-event time), \verb{twoN_<seg>} (population
size), \verb{m_<dest>} (mixFrac), plus the \code{one}/\code{shared} twoN sentinels. A
\code{.legofit} produced from a hand-written \code{.lgo} with arbitrary parameter names
(e.g. \code{Tnd}, \code{Tmnd}) parses correctly but every parameter classifies as
\code{"unknown"} and cannot be mapped onto graph edges. To read a third-party fit
into an edge tibble, export the matching topology with \code{\link[=graph_to_lgo]{graph_to_lgo()}} so the
names follow the convention.
}

\section{Fitted columns (when \code{graph} is supplied)}{

\describe{
\item{\code{time}}{Fitted \verb{T_<to>} for each edge's younger endpoint.}
\item{\code{admix_event_time}}{Fitted \verb{T_admix_<to>} for admix-typed edges.}
\item{\code{twoN}}{Fitted effective population size for \verb{<to>}.}
\item{\code{admix_prop}}{Fitted mixFrac (\verb{m_<to>}) for admix-typed edges.}
\item{\code{admix_event}}{The mixFrac parameter name (grouping ID).}
}
}

\section{Attributes}{

\describe{
\item{\code{node_times}}{Named numeric vector of fitted \verb{T_<n>} for ALL graph
nodes, including the root and other nodes that have no incoming edge and
therefore do not appear in the edge-tibble \code{time} column. Example:
\preformatted{
      result <- read_legofit_output("foo.legofit", graph = g)
      attr(result, "node_times")
      #>  xyz    xy     x     y     z
      #> 2.00  0.50  0.00  0.00  0.00
    }
}
\item{\code{fit_convergence}}{List with \code{status}, \code{cost}, \code{spread} from the
DiffEv summary line. \code{status} is \code{"reached_goal"} (converged),
\code{"finished_iterations"} (hit iteration cap), or \code{NA} (unknown).}
\item{\code{identifiability}}{Per-parameter tibble (\code{parameter}, \code{family},
\code{identifiability}, \code{reason}) flagging which fitted values the site-pattern
data can actually constrain. Classes: \code{"identifiable"} (data-constrained,
e.g. mixFracs and shallow split times); \code{"structural_none"} (the value is
arbitrary, e.g. admix-event times, whose single-lineage segment admits no
coalescence); \code{"scale_degenerate"} (identified only up to a global scale,
when a model has both free times and free \code{twoN} so only \code{dt/twoN} ratios
are fitted); \code{"weak_identified"} and \code{"weak_unconstrained"} (a deep tree
split time whose coalescent depth \code{t/twoN} puts it in the downward-biased
regime, with a looser onset depth when the split has a directly attached
sampled leaf child, so treat these as lower bounds); and \code{"fixed"} (an
input, not an estimate). This is a
\strong{structural} judgement from topology, fitted depths, and the model's
declarations; it is
never inferred from bootstrap CI width, because a non-identifiable
parameter can carry a narrow CI around a badly biased estimate.}
}
}

\examples{
# Construct a minimal `.legofit` output and read it back.
tmp <- tempfile(fileext = ".legofit")
writeLines(c(
  "Initial parameter values",
  "Fixed:",
  "       one = 1",
  "Free:",
  "     T_R = 2",
  "DiffEv reached_goal. cost=1e-10 spread=1e-7",
  "Fitted parameter values",
  "Free:",
  "     T_R = 2"
), tmp)
read_legofit_output(tmp)
unlink(tmp)
}
