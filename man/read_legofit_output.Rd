% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legofit.R
\name{read_legofit_output}
\alias{read_legofit_output}
\title{Read a LEGOFIT fitted-output file into an admixtools edge tibble}
\usage{
read_legofit_output(path, graph = NULL, include_fixed = TRUE)
}
\arguments{
\item{path}{Path to a \code{.legofit} file (legofit stdout, UTF-8).}

\item{graph}{An edge tibble, igraph, or \code{NULL}. When supplied, parameter
names are mapped to admixtools edge endpoints per the segment-naming
convention from \code{\link[=graph_to_lgo]{graph_to_lgo()}}. When \code{NULL}, returns a raw parameter
table.}

\item{include_fixed}{Logical (default \code{TRUE}). When \code{TRUE}, fixed parameter
values from the \strong{Initial parameter values} block are included in the
result. When \code{FALSE}, only free-fitted values are returned.}
}
\value{
A tibble (with \code{node_times} and \code{fit_convergence} attributes) when
\code{graph} is supplied; a raw parameter tibble (with \code{fit_convergence}) when
\code{graph = NULL}.
}
\description{
Parses the stdout of a \code{legofit} run into an admixtools edge tibble with
fitted parameter values attached as additional columns. When \code{graph} is
supplied, parameters are mapped to edges by segment name; when \code{NULL}, a
raw parameter tibble is returned.
}
\section{Fitted columns (when \code{graph} is supplied)}{

\describe{
\item{\code{time}}{Fitted \verb{T_<to>} for each edge's younger endpoint.}
\item{\code{admix_event_time}}{Fitted \verb{T_admix_<to>} for admix-typed edges.}
\item{\code{twoN}}{Fitted effective population size for \verb{<to>}.}
\item{\code{admix_prop}}{Fitted mixFrac (\verb{m_<to>}) for admix-typed edges.}
\item{\code{admix_event}}{The mixFrac parameter name (grouping ID).}
}
}

\section{Attributes}{

\describe{
\item{\code{node_times}}{Named numeric vector of fitted \verb{T_<n>} for ALL graph
nodes, including the root and other nodes that have no incoming edge and
therefore do not appear in the edge-tibble \code{time} column. Example:
\preformatted{
      result <- read_legofit_output("foo.legofit", graph = g)
      attr(result, "node_times")
      #>  xyz    xy     x     y     z
      #> 2.00  0.50  0.00  0.00  0.00
    }
}
\item{\code{fit_convergence}}{List with \code{status}, \code{cost}, \code{spread} from the
DiffEv summary line. \code{status} is \code{"reached_goal"} (converged),
\code{"finished_iterations"} (hit iteration cap), or \code{NA} (unknown).}
}
}

