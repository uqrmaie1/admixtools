% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nodes.R
\name{add_sampled_tips}
\alias{add_sampled_tips}
\title{Materialise sampled internal nodes as leaf tips.}
\usage{
add_sampled_tips(graph, suffix = "_anc")
}
\arguments{
\item{graph}{An edge tibble carrying a \code{nodes} attribute (e.g. from
\code{read_lgo}). Graphs with no sampled internal nodes are returned
unchanged.}

\item{suffix}{Suffix for the new ancestor node names (default \code{"_anc"}).}
}
\value{
An edge tibble in tip/sibling form, fittable by \code{qpgraph}. Every
edge column of the input is carried through; the new ancestor->tip
edges take \code{NA} for fitted quantities (e.g. \code{weight}, \code{time}) and the
input's non-admix \code{type} literal. The input's \code{nodes} attribute is
dropped, because it is keyed to the old structure and the
formerly-internal sampled nodes are now leaves.
}
\description{
qpgraph models every fitted population as a graph tip. A population
marked \code{samples} in the nodes tibble that sits at an internal
(ancestral) position cannot be fit directly: forcing it into the
population set collapses the edge to its descendants to ~0 and the
score blows up by orders of magnitude. This transform rewrites each
sampled internal node \code{X} into a new unlabelled ancestor \verb{X<suffix>}
(inheriting X's parents and children) plus \code{X} as a leaf tip off that
ancestor, the standard tip/sibling representation. After the
transform \code{X} is a topological leaf, so the existing leaf-based
fitting path uses it with no further change. A sampled internal node
that is itself an admixture node (in-degree 2) keeps both parent edges
on the new ancestor, so the admixture is preserved.
}
\seealso{
\code{\link{set_node_attrs}}
}
