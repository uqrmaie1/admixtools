% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legofit.R
\name{default_drift_to_time}
\alias{default_drift_to_time}
\title{Default drift-to-time conversion (identity)}
\usage{
default_drift_to_time(drift_vec, segment_vec = NULL)
}
\arguments{
\item{drift_vec}{numeric vector of per-edge drift values.}

\item{segment_vec}{character vector of segment names (the \code{to} endpoint of
each edge); ignored by this default but required by the contract so
user-supplied alternatives can index per-segment.}
}
\value{
Numeric vector, same length as \code{drift_vec}.
}
\description{
The default \code{drift_to_time} function for \code{\link[=graph_to_lgo]{graph_to_lgo()}}. Returns
\code{drift_vec} unchanged (treats drift values directly as LEGOFIT time
starting values).
}
\details{
Supply a custom function here when you want to convert drift values to
absolute generation times before writing the \code{.lgo}. The function must
accept two arguments: \code{drift_vec} (numeric) and \code{segment_vec} (character),
and return a numeric vector of the same length as \code{drift_vec}.
}
