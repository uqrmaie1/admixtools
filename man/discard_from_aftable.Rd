% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{discard_from_aftable}
\alias{discard_from_aftable}
\title{Filter SNPs in an allele-frequency table}
\usage{
discard_from_aftable(
  afdat,
  maxmiss = 0,
  minmaf = 0,
  maxmaf = 0.5,
  minac2 = FALSE,
  outpop = NULL,
  auto_only = TRUE,
  poly_only = FALSE,
  transitions = TRUE,
  transversions = TRUE,
  keepsnps = NULL
)
}
\arguments{
\item{afdat}{An allele-frequency table: a list with elements \code{afs}
(nsnp x npop matrix of reference-allele frequencies), \code{counts}
(nsnp x npop matrix of observed allele counts), and \code{snpfile}
(a tibble of SNP metadata with at least \code{SNP}, \code{CHR}, \code{A1}, \code{A2}).}

\item{maxmiss}{Drop SNPs where the fraction of populations with zero
called alleles exceeds this value. Default \code{0}: drop any SNP missing
from at least one population. Set to \code{1} to disable.}

\item{minmaf}{Minimum minor-allele frequency (computed as a
count-weighted row mean across populations). Default \code{0} (no filter).}

\item{maxmaf}{Maximum minor-allele frequency. Default \code{0.5}.}

\item{minac2}{If \code{TRUE} (or \code{2}), drop SNPs whose minimum per-population
allele count is below 2. With \code{minac2 = 2}, only non-singleton
populations are considered when computing the minimum. Default \code{FALSE}.}

\item{outpop}{Optional name of an outgroup population. When supplied,
adds an \code{outgroupaf} column to the SNP metadata equal to the allele
frequency of \code{outpop} at each SNP; otherwise the column is set to a
sentinel value of \code{0.5} (the midpoint between 0 and 1), which makes
any subsequent outgroup-based filter a no-op.}

\item{auto_only}{Drop SNPs on non-autosomal chromosomes (anything
outside \code{1:22}). Default \code{TRUE}.}

\item{poly_only}{Drop SNPs that are monomorphic across the included
populations. Default \code{FALSE}.}

\item{transitions}{Keep transition SNPs (A/G and C/T). Default \code{TRUE}.
Set to \code{FALSE} for ancient-DNA studies that want to exclude
transitions to avoid post-mortem C->T / G->A deamination artifacts.}

\item{transversions}{Keep transversion SNPs. Default \code{TRUE}.}

\item{keepsnps}{Optional character vector of SNP IDs to retain. When
supplied, overrides \strong{all other filters} (\code{maxmiss}, \code{auto_only},
\code{poly_only}, \code{minmaf}, \code{maxmaf}, \code{minac2}, \code{transitions},
\code{transversions}) -- only the SNPs whose IDs appear in \code{keepsnps} are
kept, regardless of any other filter setting. Default \code{NULL}.}
}
\value{
A list with the same names as \code{afdat} (\code{afs}, \code{counts}, \code{snpfile}),
restricted to SNPs that pass all active filters. Errors if zero SNPs remain.
}
\description{
Applies SNP-level filters to an allele-frequency object (the kind returned
by \code{\link[=plink_to_afs]{plink_to_afs()}}, \code{\link[=eigenstrat_to_afs]{eigenstrat_to_afs()}}, or
\code{\link[=packedancestrymap_to_afs]{packedancestrymap_to_afs()}}) and returns the same shape with the dropped
SNPs removed. This is the filter that \code{\link[=extract_f2]{extract_f2()}} applies internally between
reading allele frequencies from disk and computing per-pair f2 blocks; it
is exported here so callers who assemble the AFS-direct pipeline manually
can apply the same filter without reaching into the package via \code{:::}.
}
\examples{
\dontrun{
# Read allele frequencies for two populations from a PLINK1 dataset,
# then drop any SNP that is missing in at least one population and any
# non-autosomal site:
afdat = plink_to_afs("/path/to/prefix", pops = c("PopA", "PopB"))
afdat_filt = discard_from_aftable(afdat, maxmiss = 0, auto_only = TRUE)
nrow(afdat_filt$afs) <= nrow(afdat$afs)   # TRUE
}
}
