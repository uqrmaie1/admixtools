% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json_output.R
\name{read_f2_cache_metadata}
\alias{read_f2_cache_metadata}
\title{Read the cache metadata sidecar written by extract_f2()}
\usage{
read_f2_cache_metadata(outdir)
}
\arguments{
\item{outdir}{Path to the f2 output directory passed to \code{\link[=extract_f2]{extract_f2()}}.}
}
\value{
A named list with the fields described above.
}
\description{
Reads the \code{cache_metadata.json} file written by \code{\link[=extract_f2]{extract_f2()}} into the
f2 output directory and returns it as a named R list. This gives wrappers
and orchestrators a stable API for post-build telemetry (\code{n_snps},
\code{n_blocks}, \code{pops}, ...) without reaching into the internal \verb{*.rds} layout.
}
\details{
The exact set of fields depends on which code path produced the cache:
\itemize{
\item \strong{Regular path} (\code{qpfstats = FALSE}): \code{schema_version},
\code{admixtools_version}, \code{built_at}, \code{pops}, \code{n_snps}, \code{n_blocks},
\code{blgsize}, \code{maxmiss}, \code{minmaf}, \code{maxmaf}, \code{minac2}, \code{auto_only},
\code{transitions}, \code{transversions}, \code{adjust_pseudohaploid}, \code{poly_only},
\code{apply_corr}, \code{afprod}, \code{fst}, \code{qpfstats} (= \code{FALSE}), \code{cache_id}.
\item \strong{qpfstats path} (\code{qpfstats = TRUE}): \code{schema_version},
\code{admixtools_version}, \code{built_at}, \code{pops}, \code{n_snps}, \code{n_blocks},
\code{blgsize}, \code{maxmiss}, \code{adjust_pseudohaploid}, \code{qpfstats} (= \code{TRUE}),
\code{cache_id}. Filter arguments that qpfstats handles internally
(\code{minmaf}, \code{maxmaf}, \code{auto_only}, \code{transitions}, etc.) are omitted
because they don't apply on this code path.
}

\code{cache_id} is \code{NULL} (rendered as JSON \code{null}) if the cache-id
computation failed at build time; \code{built_at} is ISO 8601 with
millisecond precision in UTC. The \code{cache_id} value mirrors the
\code{.f2_cache_id} sidecar contents.
}
\seealso{
\code{\link[=extract_f2]{extract_f2()}}, \code{\link[=compute_f2_cache_id]{compute_f2_cache_id()}}, \code{\link[=result_to_json]{result_to_json()}}
}
