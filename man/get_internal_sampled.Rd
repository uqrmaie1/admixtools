% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nodes.R
\name{get_internal_sampled}
\alias{get_internal_sampled}
\title{Names of \code{samples}-marked nodes that sit at an internal (non-leaf) position.}
\usage{
get_internal_sampled(graph)
}
\arguments{
\item{graph}{An edge tibble (data.frame). Non-data.frame input, or a
graph whose nodes tibble lacks a \code{samples} column, yields an empty
vector.}
}
\value{
Character vector of node names (possibly empty).
}
\description{
The single predicate behind \code{add_sampled_tips} and the \code{qpgraph}
sampled-internal guard, so the two never disagree about which nodes
are sampled internals. A node qualifies when
its nodes-tibble \code{samples} value is a positive count (both \code{NA} and \code{0}
mean "not sampled", the \code{read_lgo} convention), it has an incident edge
(orphan nodes-tibble rows are ignored), and it is not already a
topological leaf. Leaves are taken by name as \code{setdiff(to, from)}, so
this runs on the raw edge tibble with no igraph conversion.
}
\keyword{internal}
