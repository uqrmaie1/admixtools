% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legofit.R
\name{read_legofit_bootstrap}
\alias{read_legofit_bootstrap}
\title{Read a bootci.py bootstrap output file into a tidy CI table}
\usage{
read_legofit_bootstrap(path, graph = NULL)
}
\arguments{
\item{path}{Path to a \code{bootci.py} output file (UTF-8 encoded).}

\item{graph}{An edge tibble, igraph, or \code{NULL}. When supplied, ghost-segment
parameters are dropped (no-op for standard LEGOFIT output) and parameter
families are classified.}
}
\value{
A tibble with columns \code{parameter}, \code{family}, \code{point_estimate},
\code{lo}, \code{hi}. If \verb{bootci.py -l <label>} was used, an additional \code{lbl}
column appears. The confidence level is embedded in the file as a comment
(not returned as a column).
}
\description{
Parses the output of LEGOFIT's \code{bootci.py} utility into a tidy tibble with
per-parameter bootstrap confidence intervals. The point estimate column
(\code{point_estimate}) comes from \code{bootci.py}'s \code{est} column, which reflects
the real-data fit value.
}
\details{
The confidence level used by \code{bootci.py} is stored in a comment line in the
file (e.g., \verb{# confidence: 0.950}). PR delta does not expose a \code{level}
argument; the bounds in the file are returned as-is. To change the level,
re-run \code{bootci.py} with the desired \code{--confidence} flag.
}
