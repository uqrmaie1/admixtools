% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qpadm.R
\name{qpadm_with_pruning}
\alias{qpadm_with_pruning}
\title{Iteratively prune sister-clade right pops from a qpadm fit}
\usage{
qpadm_with_pruning(
  data,
  left,
  right,
  target = NULL,
  singular_threshold = 1e-12,
  min_right_pops = NULL,
  max_iterations = NULL,
  strategy = c("greedy", "lookahead"),
  lookahead_top_j = 3,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{The input data in the form of:
\itemize{
\item A 3d array of blocked f2 statistics, output of \code{\link{f2_from_precomp}} or \code{\link{extract_f2}}
\item A directory with f2 statistics
\item The prefix of a genotype file
}}

\item{left}{Left populations (sources)}

\item{right}{Character vector of initial right ("outgroup") populations.
\code{right[1]} is the reference anchor and cannot be pruned (see Details).
Pruning drops from \code{right[-1]}; the final retained set is reported as
\code{right_final} on the returned object.}

\item{target}{Target population, or \code{NULL} for a qpwave-style (no-target)
fit. Passed directly to \code{\link[=qpadm]{qpadm()}}. Default \code{NULL}.}

\item{singular_threshold}{Reciprocal condition number floor; the loop
terminates with \code{converged = TRUE} once \code{f4_var_rcond} clears this bar.
Unlike \code{\link[=qpadm]{qpadm()}}'s \code{singular_threshold} (which raises an error on trip),
here the threshold is the convergence criterion, not a fail-loud gate.
Default \code{1e-12}. Values above \code{1e-8} trigger a warning because inner
fits may not populate loadings in the \verb{(1e-8, singular_threshold)} band.}

\item{min_right_pops}{Minimum size of the right set; the loop terminates
with \code{converged = FALSE} and \code{reason = "floor"} if pruning would
reduce \code{right} below this floor without converging. Defaults to
\code{length(left) + (!is.null(target))}, the structural minimum for qpadm
identification. When \code{reason = "floor"}, the returned fit corresponds to
the last (near-singular) right set; inspect \verb{$f4_var_rcond} before using
the weights.}

\item{max_iterations}{Hard cap on iterations; defaults to
\code{length(right) - min_right_pops + 1}. The default is the largest
number of drops the floor allows, so it should never trip; this is a
belt-and-suspenders bound against a logic bug.}

\item{strategy}{Pruning strategy: \code{"greedy"} (default) or \code{"lookahead"}.
See Details.}

\item{lookahead_top_j}{Only used when \code{strategy = "lookahead"}. Number
of top candidates (by absolute loading) to evaluate per iteration.
Default \code{3}. Higher values trade runtime for thoroughness; values above
~5 rarely help on real panels.}

\item{verbose}{If \code{TRUE}, emits a \code{cli} info line per iteration naming
the dropped pop and rcond. Default \code{FALSE} (batch-friendly).}

\item{...}{Additional arguments forwarded to each inner \code{\link[=qpadm]{qpadm()}} call.}
}
\value{
A list with the slots of the final \code{\link[=qpadm]{qpadm()}} fit (so \code{weights},
\code{f4}, \code{rankdrop}, \code{popdrop}, \code{f4_var_rcond}, \code{f4_var_singular_loadings}
are all present) plus four pruner-specific slots:
\itemize{
\item \code{pruning_trail}: tibble with one row per drop, columns \code{iteration}
(integer), \code{dropped} (character, the pop dropped), \code{loading} (double,
absolute loading of the dropped pop on the smallest singular vector
at drop time), \code{rcond_before} (double, \code{f4_var_rcond} of the fit
just before the drop), \code{rcond_after} (double, \code{f4_var_rcond} of the
fit after the drop is committed). Empty tibble if the initial fit
was already well-conditioned.
\item \code{right_final}: character vector of right pops in the final fit
(i.e. \verb{setdiff(right, dropped pops in trail)} in original order).
\item \code{converged}: \code{TRUE} if the loop exited with \code{f4_var_rcond >= singular_threshold}, \code{FALSE} otherwise.
\item \code{reason}: character scalar explaining the terminal state. One of
\code{"converged"} (the success case), \code{"floor"} (would have dropped below
\code{min_right_pops}; returned fit is near-singular), \code{"iter_cap"}
(defensive; max_iterations reached), \code{"null_loadings"} (loadings table
missing or empty; can happen when \code{singular_threshold > 1e-8}),
\code{"stale_pop"} (the named drop pop was not in the active right set;
defensive guard), \code{"inner_error"} (an inner \code{\link[=qpadm]{qpadm()}} call errored;
a warning is emitted and the partial trail is preserved).
}

For a converged fit, calling \code{qpadm(data, left, right_final, target, ...)}
directly produces identical \code{weights}, \code{f4}, \code{rankdrop}, \code{popdrop},
and \code{f4_var_rcond} to the returned object (the pruner emits the same
call shape for the terminal fit).
}
\description{
Wraps \code{\link[=qpadm]{qpadm()}} in a loop that drops the highest-loading right population
from the f4-variance singular vector until the fit's reciprocal condition
number clears \code{singular_threshold}. Codifies the hand-curation defense
against near-singular f4 covariance matrices (the diagnostic surface from
\code{\link[=qpadm]{qpadm()}}'s \code{f4_var_rcond} + \code{f4_var_singular_loadings} outputs) as a
first-class function with a load-bearing audit trail.
}
\details{
Two pruning strategies are supported:

\describe{
\item{\code{"greedy"} (default)}{At each iteration, drop the single right pop
with the largest absolute loading on the smallest right-singular vector
of \code{f4_var}. Cost: one \code{\link[=qpadm]{qpadm()}} fit per iteration. Almost always
correct.}
\item{\code{"lookahead"}}{At each iteration, evaluate the top
\code{lookahead_top_j} candidate drops by actually performing the post-drop
\code{\link[=qpadm]{qpadm()}} fit, and pick the candidate that maximizes post-drop
\code{f4_var_rcond}. Cost: \code{lookahead_top_j + 1} fits per iteration (e.g.
4x greedy at the default \code{lookahead_top_j = 3}). Protects against the
"sister-pair masking" edge case: when two right pops have near-equal
loadings but very different post-drop rconds (e.g. one is the true
sister-of-source while the other was sister-of-the-first), greedy
\code{which.max} can pick the wrong one and chain into over-pruning;
lookahead sees the post-drop landscape and picks the genuinely
informative drop.}
}

The pruner commits to a drop at each step and does not backtrack across
iterations. For tightly-constrained panels where a globally-optimal subset
matters more than runtime, a full branch-and-bound enumeration would be the
next escalation; that is out of scope for v1.

\strong{\code{right[1]} is the reference anchor and cannot be pruned.} \code{\link[=qpadm]{qpadm()}}'s f4
parameterization is \verb{f4(left[1], left[-1]; right[1], right[-1])}, and the
SVD attribution covers only \code{right[-1]}. A near-collinear \code{right[1]} would
not appear in the loadings table and the pruner cannot detect or remove it.
Convention is to place a phylogenetically-distant, unambiguous outgroup
(e.g. \code{Chimp.REF}) at \code{right[1]}. If you suspect \code{right[1]} is the
offender, try rotating it with a different pop and re-running.

\strong{Performance note for genotype-prefix data.} When \code{data} is a file-system
prefix rather than a pre-computed \code{f2_blocks} array, each inner \code{\link[=qpadm]{qpadm()}}
call reads from disk. With \code{lookahead_top_j = 3} and ten iterations, that
is up to 40 disk reads. Pre-compute with \code{\link[=extract_f2]{extract_f2()}} and pass the
resulting array for batch work.

Defensive guards return early with \code{converged = FALSE} and a populated
\code{reason} slot (rather than guessing) if \code{\link[=qpadm]{qpadm()}}'s diagnostic surface
returns something the pruner cannot interpret: an empty / \code{NULL}
loadings table, a named drop pop that's not in the active right set
(stale loading from a prior fit), the floor \code{min_right_pops} being hit
before convergence, or the iteration cap being reached.
}
\examples{
\dontrun{
# Pruning loop converges in one step on a sister-pair fixture:
fit = qpadm_with_pruning(
  example_f2_blocks,
  left   = c("Russia_Ust_Ishim.DG", "Vindija.DG"),
  right  = c("Chimp.REF", "Altai_Neanderthal.DG", "Mbuti.DG",
             "Switzerland_Bichon.SG"),
  target = "Denisova.DG")
fit$converged          # TRUE
fit$pruning_trail      # tibble: which pop was dropped, when, why
fit$right_final        # the converged right set
}
}
\seealso{
\code{\link[=qpadm]{qpadm()}}, \code{\link[=qpadm_multi]{qpadm_multi()}}, \code{\link[=qpadm_sweep]{qpadm_sweep()}}
}
